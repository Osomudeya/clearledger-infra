# LOCAL / STAGE 0–4 ONLY — not synced to clearledger-infra after Stage 5.
apiVersion: v1
kind: Secret
metadata:
  name: ledger-service-secret
  namespace: clearledger
type: Opaque
stringData:
  database_url: "postgresql://clearledger:changeme-stage0@postgres:5432/clearledger"
