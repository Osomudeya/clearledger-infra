# INTRODUCED: Stage 0 — Raw Kubernetes
# LOCAL / STAGE 0–4 ONLY — do NOT copy this file into clearledger-infra after Stage 5.
# ArgoCD must not apply it once Vault is the source of truth (delete from infra repo).
apiVersion: v1
kind: Secret
metadata:
  name: auth-service-secret
  namespace: clearledger
type: Opaque
stringData:
  database_url: "postgresql://clearledger:changeme-stage0@postgres:5432/clearledger"
  jwt_secret: "stage0-jwt-secret-change-in-production"
