# INTRODUCED: Stage 0 — Raw Kubernetes
# REMOVED: Stage 5 — replaced by Vault agent injection
# PURPOSE: Auth service credentials as a Kubernetes Secret
# WARNING: plaintext stringData is readable by anyone with kubectl access.
#          This file is the problem Stage 5 solves.
apiVersion: v1
kind: Secret
metadata:
  name: auth-service-secret
  namespace: clearledger
type: Opaque
stringData:
  database_url: "postgresql://clearledger:changeme-stage0@postgres:5432/clearledger"
  jwt_secret: "stage0-jwt-secret-change-in-production"
