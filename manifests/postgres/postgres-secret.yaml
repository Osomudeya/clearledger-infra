# INTRODUCED: Stage 0 — Raw Kubernetes
# PURPOSE: Database credentials as a Kubernetes Secret
# WARNING: This is intentionally not production-grade.
#          Secrets stored here are base64-encoded in etcd — not encrypted.
#          You will feel this problem. Vault fixes it in Stage 5.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: clearledger
type: Opaque
data:
  # echo -n "clearledger" | base64
  username: Y2xlYXJsZWRnZXI=
  # echo -n "changeme-stage0" | base64
  password: Y2hhbmdlbWUtc3RhZ2Uw
